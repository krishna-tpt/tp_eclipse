# ═══════════════════════════════════════════════════════
#  Neon DB Cloud Sync Configuration
#  File: src/main/resources/neon_config.properties
#  (or set as JVM -D args in Tomcat startup)
# ═══════════════════════════════════════════════════════

# Neon PostgreSQL connection URL
# Format: jdbc:postgresql://<host>.neon.tech/<dbname>?sslmode=require
neon.jdbc.url=jdbc:postgresql://ep-plain-meadow-apr9n2ix-pooler.c-7.us-east-1.aws.neon.tech/mydb?sslmode=require

# Neon DB credentials
neon.jdbc.user=neondb_owner
neon.jdbc.password=npg_30bZIuKdaFvM

# ── How to set these as JVM args in Eclipse Tomcat ──────
# Window → Preferences → Server → Runtime Environments
# → Edit → (double-click your Tomcat) → Open launch config
# → Arguments tab → VM arguments:
#
# -Dneon.jdbc.url=jdbc:postgresql://ep-xxxx.neon.tech/neondb?sslmode=require
# -Dneon.jdbc.user=neondb_owner
# -Dneon.jdbc.password=YOUR_PASSWORD
#
# ── OR set as environment variables ─────────────────────
# NEON_DB_URL=jdbc:postgresql://ep-xxxx.neon.tech/neondb?sslmode=require
# NEON_DB_USER=neondb_owner
# NEON_DB_PASSWORD=YOUR_PASSWORD
