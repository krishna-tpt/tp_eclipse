<?xml version="1.0" encoding="UTF-8"?>
<!--
  Single text appender. The previous <if><then><else> JSON/text toggle required
  Janino on the classpath; dropped to keep the dependency footprint minimal.
  Tooling layers (filebeat, journald) can convert text to JSON downstream.
-->
<configuration>

    <appender name="STDOUT" class="ch.qos.logback.core.ConsoleAppender">
        <encoder>
            <pattern>%d{yyyy-MM-dd HH:mm:ss.SSS} %-5level [%thread] %logger{36} - %msg%n</pattern>
        </encoder>
    </appender>

    <root level="${LOG_LEVEL:-INFO}">
        <appender-ref ref="STDOUT"/>
    </root>

    <logger name="org.michelin.filemanager" level="${APP_LOG_LEVEL:-INFO}"/>
    <logger name="org.apache.sshd" level="WARN"/>
    <logger name="org.flywaydb"   level="INFO"/>

</configuration>
