interface: MICH_INV_STOCKLEVEL
variant:   BATCH_408
version:   "1.0"
matches:
  filename_pattern: "^a$"
parser:
  delimiter: ";"
  quote:     "\""
  encoding:  UTF-8
envelope:
  - { tag: HEADER_FILE, fields: 1 }
records:
  - tag: A
    fields: 2
    target_table: staging.x
    columns:
      a_col: { field: 1 }
