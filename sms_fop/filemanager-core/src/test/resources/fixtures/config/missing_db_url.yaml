# Fixture: db.url is absent. ConfigLoader must fail fast with a named error.
profile: test

db:
  user:     postgres
  password: postgres

file:
  source: local
  local:
    pickup_path:  ./inbound
    archive_path: ./archive
    reject_path:  ./rejected

notifier:
  webhook_urls:
    - http://localhost:8080/webhook
