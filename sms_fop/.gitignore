# Gradle
.gradle/
build/
!gradle/wrapper/gradle-wrapper.jar

# Legacy Maven output — still generated by the frozen v6 test-data-generator
# under deploy/11-06-v6-customer/. Ignore anywhere it shows up.
target/

*.iml

# IDE
.idea/
.vscode/
.classpath
.project
.settings/
.factorypath
*.swp
*~

# Build artifacts
*.class

# GitLab push rule blocks binary bundles — never commit these.
# If you need a zip inside the repo for reference, submit it via a
# release asset / artifact store instead.
*.zip
*.tar
*.tar.gz
*.rar
*.7z

# Logs
logs/
*.log

# OS
.DS_Store
Thumbs.db

# Runtime / local
.env
.env.local
inbound/
archive/
rejected/
generated/

# JaCoCo
jacoco.exec
